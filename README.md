# UMA OCR DATA

This repo contains data collected from the Umamusume CM data collection project.

#### Usage

Intended to be used alongside the uma-ocr tool

`move.py` for replicating data and sorting.

Create an ENV with with target dirs

```
OUTPUT_DIR=../phashcheck/output
DATA_REPO_PATH=.
```